### Comments/Ideas/Concepts
 * Going to make a temporary data structure:
    * LinkedList of structs(unclear what is going to be in this struct)
    * Needs a fast input, fast all out(or partial-out)
    * Do I want a bloated management struct for the linkedList 
      would that slow things down?
    * Need a fast delete option (Potentially have two data structures one for fast all-out)