### Commentary
 * I am going to propose to do an initial message connection to separate use cases
 * Tcp connection to start(may add udp functionality)
 * initial message should be used to separate into (messaging with other users, IoT p2p, file-sharing, p2p-storage, p2p-VPN)
 * Think about header space for the first message
 * Need to create semaphores/mutexes for threading